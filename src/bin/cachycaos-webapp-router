#!/usr/bin/env bash

set -euo pipefail

DATA_HOME="${XDG_DATA_HOME:-$HOME/.local/share}"
APP="$DATA_HOME/cachycaos/modules/webapps/router/app.sh"

[[ -x "$APP" ]] || {
  echo "Error: no se encontró el WebApp Router en $APP" >&2
  exit 1
}

exec "$APP" "$@"
